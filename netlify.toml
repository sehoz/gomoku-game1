[build]
  command = "npm --prefix frontend install --registry=https://registry.npmjs.org --no-audit --no-fund && npm --prefix frontend run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
